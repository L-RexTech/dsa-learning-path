/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/dsa-learning-path', // Change this to your repository name
    images: {
      unoptimized: true,
    },
  }
  
  module.exports = nextConfig